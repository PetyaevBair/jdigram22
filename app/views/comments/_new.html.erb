<%= form_with model: comment, url: post_comments_path(post_id: post, id: comment), method: "post", local: false do |form| %>
	<div>
		<p><%= form.label :text %></p>
		<p><%= form.text_area :text %></p>
	</div>
	<div>
		<p><%= form.submit "Создать комментарий" %></p>
	</div>
<% end %>