<%= form_with model: @comment, url: post_comments_path(@post, @comment), method: "post", local: false do |form| %>
	<div>
		<p><%= form.label :text %></p>
		<p><%= form.text_field :text %></p>
	</div>
	<div>
		<p><%= form.submit "Создать комментарий" %></p>
	</div>
<% end %>