\///$("#comments_grid").html("#{escape_javascript(render partial: '/posts/partials/comment_list', locals: { post: @post } )}");
$("#comment_box_#{@post.id}").append('#{j render partial: "/posts/partials/last_comment", locals: { post: @post, comment: @comment }}');
$("#comment-text-#{@post.id}").val('');
